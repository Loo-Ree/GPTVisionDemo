credentials:
  usernames:
    user:
      email: user@email.com
      name: user
      password: YOUR HASHED PASSWORD GOES HERE # hashed password - i.e. print(stauth.Hasher(['abc']).generate())
cookie:
  expiry_days: 30
  key: axu927c81zt # Must be string
  name: streamlitauth_cookie
preauthorized:
  emails: