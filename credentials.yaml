credentials:
  usernames:
    gptuser:
      email: lr@microsoft.com
      name: Amico
      password: YOUR-PASSWORD-HASH-GOES-HERE # hashed password - i.e. print(stauth.Hasher(['abc']).generate())
cookie:
  expiry_days: 30
  key: iwuj19c81pt # Must be string
  name: streamlitauth_cookie
preauthorized:
  emails: